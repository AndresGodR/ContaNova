export const metadata = {
  title: "Admin | Godínez & Asociados",
};

export default function AdminPage() {
  return (
    <main className="container-max section-padding">
      <h1 className="text-3xl font-bold text-primary">Panel de Dashboard</h1>
      <p className="mt-3 text-gray-600">
        Área interna (en construcción). Aquí irá el portal, reportes, módulos, etc.
      </p>
    </main>
  );
}
